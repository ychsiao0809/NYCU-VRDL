# NYCU-VRDL

## Intallation
```
git clone --recurse-submodules https://github.com/ychsiao0809/NYCU-VRDL.git
cd 21LPCVC-UAV_VIdeo_Track-Sample-Solution
meow
```

## Usage
```
usage: inference.py [-h] [--save] [--load LOAD]

optional arguments:
  -h, --help   show this help message and exit
  --save       save state of model
  --load LOAD  load model with state file
```